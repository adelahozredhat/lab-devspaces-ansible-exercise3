# cspell: disable=devspaces
# Para units en esta imagen: usar "ansible-test units --venv ..." (venv sin pytest-ansible del sistema).
schemaVersion: 2.2.2
metadata:
  name: ansible-demo
components:
  - name: tooling-container
    container:
      image: ghcr.io/ansible/ansible-devspaces@sha256:e56d86ca04d9d84d3079b12c227cc53b08a11a580d532f5a63c8e16cfddeb3ba
      memoryRequest: 256M
      memoryLimit: 6Gi
      cpuRequest: 250m
      cpuLimit: 2000m
      args: ['tail', '-f', '/dev/null']
      env:
        - name: 'ANSIBLE_COLLECTIONS_PATH'
          value: '~/.ansible/collections'
        - name: 'KUBEDOCK_ENABLED'
          value: "true"
